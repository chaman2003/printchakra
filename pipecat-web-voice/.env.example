# Ollama Configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b-instruct-q4_K_M

# Whisper Configuration
WHISPER_MODEL=medium
WHISHER_LANGUAGE=auto

# Facebook MMS TTS Configuration
MMS_MODEL_ID=facebook/mms-tts
MMS_SAMPLE_RATE=16000

# Server Configuration
HOST=0.0.0.0
PORT=8765
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Voice Bot Configuration
DEFAULT_LANGUAGE=en
SUPPORTED_LANGUAGES=en,hi,te,kn
VAD_THRESHOLD=0.5
VAD_PAD_DURATION_MS=300

# Logging
LOG_LEVEL=INFO

# Optional: PRELOAD_VOICE_MODELS=0 skips Whisper/MMS at startup (badges stay NOT LOADED until first speech)
